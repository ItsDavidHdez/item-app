import React from "react";
import { View, ScrollView, Text, Button } from "react-native";

const Home = (props) => {
  return (
    <ScrollView>
      <View>
        <Text>Add Inventory</Text>
      </View>
    </ScrollView>
  );
};

export default Home;
